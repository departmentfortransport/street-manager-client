{
  "name": "street-manager-client",
  "version": "1.0.0",
  "description": "The client used to communicate with the street-manager-api",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/departmentfortransport/street-manager-client.git"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "audit": "nsp check",
    "lint": "tslint --project tslint.json",
    "clean": "rimraf dist",
    "build": "npm run clean && npm run test && tsc",
    "test": "npm run lint"
  },
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0",
    "http-status-codes": "^1.3.0",
    "street-manager-interfaces": "file:../street-manager-interfaces"
  },
  "devDependencies": {
    "nsp": "^3.2.1",
    "tslint": "^5.9.1",
    "rimraf": "^2.6.2",
    "typescript": "^2.7.1"
  }
}
